# 毕业设计知识补课文档

这份文档的目标不是让你背公式，而是让你能用自己的话解释项目中的核心概念。每个概念都按“定义、解释、答辩可用说法”组织，答辩时优先使用最后一行的表述。

## 1. SAR 图像

**一句话定义**：SAR 是合成孔径雷达图像，主要反映地物对雷达波的散射特性。

SAR 图像不是普通相机拍出来的图，它依靠雷达主动发射电磁波并接收回波成像。它对地表粗糙度、材质、结构方向很敏感，所以建筑、道路、水体、农田在 SAR 中的表现和光学图像差异很大。SAR 的优点是不依赖太阳光，也能一定程度穿透云雾，但图像中常有斑点噪声，视觉上不直观。

**答辩可用说法**：SAR 图像和光学图像不是同一种成像机制，所以同一个地物在两类图像中的纹理和亮度差异很大，这是跨模态配准困难的根源。

## 2. 光学图像

**一句话定义**：光学图像是接近人眼视觉的遥感图像，主要反映地物对可见光或近红外波段的反射。

光学图像中道路、建筑、植被、水体通常更符合人的直观理解。它的纹理、颜色和边缘更适合常规特征匹配算法。缺点是容易受云、雾、光照和季节变化影响，数据质量不稳定。

**答辩可用说法**：真实光学图像是本系统的目标参考图，我最终希望把 SAR 图像对齐到这个坐标系下。

## 3. 跨模态配准

**一句话定义**：跨模态配准是把来自不同成像机制的图像对齐到同一空间位置。

普通图像配准通常假设两张图的纹理、边缘和亮度有一定相似性。SAR 与光学图像之间这种相似性较弱，所以直接用传统特征点匹配容易出现少匹配、错匹配或匹配点集中。跨模态配准的难点不是简单地找几何变换，而是先要找到可信的对应关系。

**答辩可用说法**：我的任务不是普通同模态配准，而是 SAR 到光学的跨模态配准，所以需要先降低模态差异，再做几何匹配。

## 4. 伪光学图像 Fake Optical

**一句话定义**：Fake Optical 是扩散模型根据 SAR 图像生成的、外观接近光学图像的中间结果。

Fake Optical 不是最终配准结果，也不是要替代真实光学图像。它的作用是作为跨模态桥梁，把 SAR 的结构信息转成更适合 LightGlue 匹配的图像形式。因为 Fake Optical 是由 SAR 条件生成的，所以它与 SAR 在像素坐标上保持对应关系，可以把匹配点迁移回 SAR。

**答辩可用说法**：Fake Optical 是桥梁，不是终点；我用它帮助匹配器找到 Fake Optical 与真实光学图之间的对应关系，再把几何关系应用回 SAR。

## 5. 扩散模型

**一句话定义**：扩散模型是一类从噪声逐步生成图像的深度生成模型。

在本项目中，我使用的是已经训练好的条件扩散模型，它的条件输入是 SAR 图像，输出是伪光学图像。这里的重点不是从零训练一个扩散模型，而是把预训练生成器接入配准系统，并验证它是否能帮助后续匹配。扩散步数越多通常生成越充分，但耗时也更高，所以项目中通过实验选择了较少步数用于演示和推理。

**答辩可用说法**：我使用扩散模型的目的不是单纯生成好看的图，而是生成一个更适合特征匹配的中间模态。

## 6. 预训练权重

**一句话定义**：预训练权重是别人或已有实验提前训练好的模型参数。

本项目使用的生成器基线是 `checkpoint-25001-lcm-adv/model.safetensors`。使用预训练权重可以避免在有限时间和有限显卡条件下从零训练扩散模型。我的工作重点是数据整理、推理接入、参数搜索、匹配评估、质量门控和 Web 系统集成。

**答辩可用说法**：预训练权重不是我的主要创新点，我的重点是把它作为生成器接入完整的 SAR-光学配准流程，并通过实验验证这个流程是否有效。

## 7. LightGlue

**一句话定义**：LightGlue 是一种图像特征匹配方法，用来判断两张图中的哪些关键点互相对应。

LightGlue 本身不负责生成图像，它负责在两张输入图之间建立匹配关系。本项目中 LightGlue 的输入是 Fake Optical 和真实 Optical，而不是直接输入 SAR 和 Optical。它输出的匹配点会被用于估计 Homography，从而得到几何变换。

**答辩可用说法**：LightGlue 在系统中承担匹配器角色，它把 Fake Optical 和真实光学图中的对应点找出来，为后续几何估计提供依据。

## 8. SuperPoint

**一句话定义**：SuperPoint 是一种关键点检测和描述子提取方法。

LightGlue 需要先有图像关键点和描述子，SuperPoint 就是常用的前端特征提取器之一。它速度较快，在本项目的实验中单独表现也比较稳定。系统默认先使用 SuperPoint，是因为它适合作为第一阶段快速匹配器。

**答辩可用说法**：SuperPoint 负责先找图像中的关键点和特征描述，LightGlue 再基于这些特征做匹配。

## 9. ALIKED

**一句话定义**：ALIKED 是另一种局部特征提取方法，可作为 SuperPoint 的补充。

不同特征提取器对不同纹理和结构的敏感性不同。实验中发现 SuperPoint 和 ALIKED 有一定互补性，有些样本 SuperPoint 不稳定时 ALIKED 可以补救。因此系统采用 SuperPoint -> ALIKED 的级联策略，而不是只依赖单一特征器。

**答辩可用说法**：ALIKED 的作用是补充 SuperPoint，提升边界样本中找到可靠匹配的概率。

## 10. Homography 单应矩阵

**一句话定义**：Homography 是描述两个平面图像之间透视变换关系的 3x3 矩阵。

当图像区域近似平面，或视角差异不特别复杂时，可以用 Homography 表示两张图之间的几何对应。系统用 LightGlue 得到的匹配点估计 Homography，再用它把 SAR 图像变换到真实光学图坐标系。它不是万能的，对强非刚性变化或地形起伏较大的区域会有限制。

**答辩可用说法**：Homography 是我把匹配点转化为整幅图像配准结果的关键几何模型。

## 11. RANSAC

**一句话定义**：RANSAC 是一种在存在错误匹配时估计模型参数的鲁棒方法。

LightGlue 输出的匹配点不一定全部正确，其中会有外点。RANSAC 会反复从匹配点中抽样，寻找能被最多点支持的几何模型。最终支持模型的点叫内点，不支持的点叫外点。

**答辩可用说法**：RANSAC 的作用是从可能有错误的匹配点中筛出可信的几何关系。

## 12. Inliers 内点

**一句话定义**：内点是符合估计几何变换的匹配点。

如果一个匹配点对在 Homography 变换后误差较小，就可以认为它支持当前几何关系。内点越多，说明匹配关系越有统计支撑。单看内点数量不够，还要看内点率和空间分布。

**答辩可用说法**：内点表示真正支持配准几何关系的匹配点，是判断结果可信度的重要指标。

## 13. Inlier Ratio 内点率

**一句话定义**：内点率是内点数量占全部匹配点数量的比例。

如果匹配点很多但大部分都是外点，配准结果仍然不可靠。内点率能反映匹配结果中正确对应关系的比例。项目中使用内点率作为质量门控的一部分，避免外点过多时误判为成功。

**答辩可用说法**：内点率可以衡量匹配点整体质量，不只是看匹配点数量多少。

## 14. RMSE

**一句话定义**：RMSE 是均方根误差，用来衡量匹配点经过几何变换后的平均偏差。

在本项目中，RMSE 越小说明内点在几何变换后越接近目标位置。它是一个像素级误差指标，适合用于评价配准精度。项目中把 RMSE 与内点数、内点率一起使用，而不是只依赖单一指标。

**答辩可用说法**：RMSE 反映配准误差大小，单位是像素，是我评价结果精度的主要指标之一。

## 15. 质量门控

**一句话定义**：质量门控是判断一次配准结果是否适合展示或采用的一组规则。

本系统不会只要 LightGlue 输出结果就认为配准成功。它会检查内点数量、内点率、RMSE、内点空间分布和 Homography 形状是否合理。这样可以减少错误配准被误认为成功的风险。

**答辩可用说法**：质量门控体现的是工程可靠性，我不只生成结果，还要判断结果是否可信。

## 16. 参数搜索

**一句话定义**：参数搜索是在多组参数下重复实验，选择效果和效率更平衡的配置。

本项目搜索了扩散步数、LightGlue 特征点数量和不同 matcher 策略。参数不是凭感觉定的，而是在过滤后的样本池上做对比。最终默认参数服务于演示稳定性和推理效率。

**答辩可用说法**：我的默认参数不是手调出来的，而是通过样本过滤和参数搜索得到的。

## 17. 样本过滤

**一句话定义**：样本过滤是从评估样本中剔除海洋、大水面、空白和低纹理区域。

这类区域本身缺少稳定结构，难以产生可靠匹配点。把它们直接纳入参数搜索，会让结果更像是在测试空白区域，而不是测试有效配准场景。项目中通过边缘强度、灰度标准差、水体 proxy 和 SAR 纹理指标筛选样本。

**答辩可用说法**：样本过滤不是删数据，而是让参数选择更聚焦于有实际配准意义的结构区域。

## 18. Web 系统集成

**一句话定义**：Web 系统把上传、推理、可视化、指标展示和实验依据整合成一个可操作平台。

如果只有脚本，导师很难直观看到系统是否真的能跑通。Web 页面提供内置样例、自定义上传、结果图、匹配证据、质量门控和实验依据展示。它让算法流程从离线实验变成可以演示、复核和解释的系统。

**答辩可用说法**：Web 系统体现的是工程落地能力，它把扩散生成和 LightGlue 配准封装成了可交互的演示平台。

## 19. 内置答辩样例

**一句话定义**：内置答辩样例是提前跑好的代表性结果，用于现场稳定展示。

现场实时推理可能受显卡、环境和依赖影响，内置样例可以保证演示稳定。每个样例都包含 SAR、真实光学、Fake Optical、匹配图、配准图和指标。它不是替代真实推理，而是用于说明系统输出长什么样。

**答辩可用说法**：内置样例用于答辩展示稳定性，自定义上传路径仍然保留完整推理能力。

## 20. 本项目的工作边界

**一句话定义**：工作边界是明确哪些部分是复用，哪些部分是集成、改进和验证。

本项目复用了预训练扩散生成器和 LightGlue 相关方法，没有声称从零提出新的生成模型或匹配网络。主要工作在于构建 SAR 到 Fake Optical 到真实光学配准的工程链路，并围绕它完成样本过滤、参数搜索、级联策略、质量门控和 Web 展示。答辩时应该清楚表达这个边界，避免把复用工作说成自研算法。

**答辩可用说法**：我复用了成熟模型作为基础，重点完成跨模态配准流程设计、工程集成、实验验证和可视化系统实现。
