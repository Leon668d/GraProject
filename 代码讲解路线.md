# 代码讲解路线

这份文档用于向导师讲解代码结构。不要逐行讲代码，按模块讲“这个文件负责什么、输入是什么、输出是什么、为什么需要它”。

## 0. 总体讲法

先用一句话概括系统：

```text
这个系统把 SAR-光学跨模态配准拆成五步：前端接收输入和展示结果，后端管理任务和 API，worker 执行扩散生成与 LightGlue 配准，实验脚本支撑参数选择，最后通过 Web 页面展示配准结果和实验依据。
```

讲代码时建议按这个顺序：

```text
前端 -> 后端 -> worker -> 实验脚本 -> 结果展示
```

## 1. 前端：页面结构和交互

**主要目录**：

```text
workspace/code/web_app/templates/
workspace/code/web_app/static/css/
workspace/code/web_app/static/js/
```

**职责**：前端负责把系统流程变成可操作页面。用户可以选择内置样例，也可以上传 SAR 和真实光学图像；页面会展示模型状态、当前配置、质量门控、配准结果、匹配证据和实验依据。

**重点文件**：

```text
templates/dashboard.html
templates/partials/dashboard_header.html
templates/partials/dashboard_control_panel.html
templates/partials/dashboard_result_stage.html
templates/partials/dashboard_evidence_panel.html
static/css/dashboard_console.css
static/js/dashboard.js
static/js/dashboard/render.js
static/js/dashboard/demos.js
```

**讲解方式**：

先打开页面，从用户视角讲：左侧是快速演示和上传入口，右侧是结果区和实验依据。然后再回到代码解释，Jinja 模板负责页面结构，CSS 负责答辩控制台样式，JS 负责调用 API、加载内置样例、渲染图片和指标。重点说明前端不是简单美化，而是把算法结果组织成导师能看懂的证据链。

**答辩可用说法**：

```text
前端的作用是把复杂的配准流程压缩成可演示的控制台，让导师可以看到输入图、生成图、匹配点、配准图和指标之间的关系。
```

## 2. 后端：Flask API 和任务管理

**主要文件**：

```text
workspace/code/web_app/app.py
```

**职责**：后端负责登录、上传、模型状态检查、任务目录管理、调用 worker、读取结果文件、返回前端需要的图片和指标。它是前端页面和算法脚本之间的桥梁。

**重点接口**：

```text
/dashboard
/api/model/status
/api/upload
/api/diffusion-register
/api/demo-samples
/api/demo-samples/<sample_id>
/api/experiment-summary
/api/results/<session_id>/<filename>
```

**讲解方式**：

讲 `app.py` 时不要从第一行开始。先讲 `/api/diffusion-register`：前端把 session、扩散步数、特征点数量和 matcher 策略传给后端，后端找到上传的 SAR/Optical 文件，然后调用 `register_diffusion_lightglue()`。再讲 `register_diffusion_lightglue()`：它负责拼接 worker 命令、传入模型权重路径、等待 worker 输出 JSON，最后把结果转成前端能展示的数据。

**答辩可用说法**：

```text
后端主要负责流程编排，不直接做深度模型计算。真正的扩散生成和 LightGlue 匹配放在独立 worker 中执行，这样 Web 服务和推理逻辑解耦。
```

## 3. Worker：扩散生成 + LightGlue 配准

**主要文件**：

```text
workspace/code/web_app/scripts/diffusion_lightglue_worker.py
```

**职责**：worker 是核心推理脚本。它读取 SAR 和真实光学图，调用预训练扩散模型生成 Fake Optical，再用 LightGlue 匹配 Fake Optical 和真实光学图，估计 Homography，最后把 SAR warp 到真实光学坐标系。

**关键函数**：

```text
run_generation()
run_lightglue()
run_match_candidate()
homography_shape_quality()
reliability_check()
main()
```

**讲解方式**：

先讲 `run_generation()`：它加载 `model.safetensors` 权重，把 SAR 转成 Fake Optical。再讲 `run_lightglue()`：它准备匹配输入，按 SuperPoint -> ALIKED 策略运行 LightGlue，并选择质量更好的候选。最后讲质量门控：系统检查内点数、内点率、RMSE、空间覆盖和 Homography 形状，决定结果是可靠候选还是待复核。

**答辩可用说法**：

```text
worker 是算法主链路，完成 SAR 到 Fake Optical 的翻译、Fake Optical 到真实光学图的匹配，以及几何关系向 SAR 的迁移。
```

## 4. 实验脚本：样本过滤、参数搜索和边界分析

**主要目录**：

```text
workspace/code/web_app/scripts/
```

**重点文件**：

```text
filter_registration_pairs.py
sweep_diffusion_lightglue_params.py
make_failure_contact_sheet.py
run_matcher_compare_100.bat
```

**职责**：实验脚本支撑系统默认参数的选择。它们不是 Web 实时推理的一部分，但用于证明为什么选择当前配置，以及为什么需要样本过滤和级联 matcher。

**讲解方式**：

先讲 `filter_registration_pairs.py`：它根据边缘强度、灰度标准差、水体 proxy 和 SAR 纹理剔除海洋、空白、低纹理样本。再讲 `sweep_diffusion_lightglue_params.py`：它在过滤后的样本上比较不同扩散步数、特征点数量和 matcher 策略。最后讲 `make_failure_contact_sheet.py`：它把边界案例做成 contact sheet，帮助分析为什么一些样本需要质量门控。

**答辩可用说法**：

```text
实验脚本的作用是让系统默认参数有依据，而不是靠主观手调。它们支撑了 steps=8、keypoints=2048、SuperPoint -> ALIKED cascade 这些默认选择。
```

## 5. 结果展示：内置样例和实验依据

**主要目录**：

```text
workspace/code/web_app/static/demo_samples/
workspace/diffusion_lightglue_cascade_study_100/
workspace/diffusion_lightglue_param_sweep/
workspace/acd_pretrained_registration_baseline/
```

**职责**：这些目录保存答辩展示需要的静态结果和实验证据。内置样例用于快速展示完整流程，实验 CSV/JSON/Markdown 用于说明参数搜索和 matcher 对比结论。

**讲解方式**：

打开 Web 页面后，先加载一个稳定样例，展示 SAR、Fake Optical、真实光学图、LightGlue 匹配、SAR 配准结果、棋盘格和融合图。然后切到实验依据区域，说明系统比较过不同 matcher 和参数组合。最后强调边界案例不是系统失败的遮掩，而是质量门控的设计依据。

**答辩可用说法**：

```text
结果展示部分把算法输出转成可视化证据，包括匹配点、配准图、棋盘格、融合图和统计指标，方便导师复核结果是否可信。
```

## 6. 一条完整代码链路怎么讲

可以按下面这条路径从页面讲到算法：

```text
dashboard.html
-> static/js/dashboard.js
-> /api/diffusion-register
-> app.py: diffusion_register_images()
-> app.py: register_diffusion_lightglue()
-> scripts/diffusion_lightglue_worker.py
-> diffusion_lightglue_result.json
-> 前端渲染结果图和指标
```

这条链路能说明前端、后端、worker 和结果展示是如何串起来的。

## 7. 导师追问时的回答边界

如果导师问“这个模型是不是你自己提出的”，回答：

```text
不是，我复用了预训练扩散模型和 LightGlue 相关方法。我的工作重点是把它们组合成 SAR-光学配准系统，并完成数据筛选、参数搜索、质量门控、结果展示和工程验证。
```

如果导师问“你能不能逐行解释代码”，回答：

```text
我可以按模块解释代码职责和数据流。这个系统的关键不是某一行语法，而是前端、后端、推理 worker 和实验脚本如何共同完成配准流程。
```

如果导师问“系统最核心的代码在哪里”，回答：

```text
核心推理在 scripts/diffusion_lightglue_worker.py，系统编排在 app.py，前端展示在 static/js/dashboard 和 templates/partials。
```

## 8. 讲解时不要犯的错误

不要说“扩散模型是我从零训练出来的”，因为当前主线使用的是预训练权重。

不要说“所有样本都能成功配准”，因为跨模态配准本身存在低纹理、生成不稳定和几何模型限制。

不要只说“我做了 Web 页面”，应该说“我做了可运行的配准系统和实验展示平台”。

不要把代码量当作唯一工作量，应该把方法链路、实验验证和系统集成一起讲。
