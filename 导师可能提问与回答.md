# 导师可能提问与回答

这份文档用于答辩前模拟问答。每个问题先给出简短回答，再给出展开回答；如果导师追问，就用展开回答补充。

## 1. 为什么不用传统方法直接匹配 SAR 和光学图？

**简短回答**：因为 SAR 和光学图像成像机制不同，纹理和亮度差异很大，传统特征匹配容易产生少匹配或错匹配。

**展开回答**：传统方法通常假设两张图有相似的边缘、纹理或灰度分布，但 SAR 图像反映的是雷达散射，光学图像反映的是地物反射，两者外观差异明显。直接用 ORB、SIFT 或普通特征匹配方法，在水体、农田、城市边缘等场景中容易得到不稳定匹配。因此本项目先把 SAR 转成 Fake Optical，降低模态差异后再匹配。

## 2. 为什么要用扩散模型？

**简短回答**：扩散模型可以把 SAR 翻译成外观更接近光学图像的 Fake Optical，降低跨模态匹配难度。

**展开回答**：本项目使用扩散模型不是为了单纯生成好看的图片，而是为了构造一个中间桥梁。Fake Optical 与 SAR 在像素坐标上保持对应关系，同时外观更接近真实光学图，因此更适合 LightGlue 找对应点。这样可以把原本困难的 SAR-光学匹配，转化为 Fake Optical-真实光学匹配。

## 3. 预训练权重是不是你自己训练的？

**简短回答**：不是从零训练的，我使用的是已有预训练生成器权重；我的工作重点是把它接入配准系统并验证有效性。

**展开回答**：扩散模型从零训练需要大量数据和显卡时间，在毕业设计周期内不现实。因此我选择预训练权重作为生成器基线，并围绕它完成数据适配、推理脚本、LightGlue 配准、参数搜索、质量门控和 Web 展示系统。答辩时需要明确：预训练生成器是基础，工程链路设计与实验验证是我的主要工作。

## 4. LightGlue 在系统中起什么作用？

**简短回答**：LightGlue 负责在 Fake Optical 和真实光学图之间寻找匹配点。

**展开回答**：扩散模型只负责生成 Fake Optical，但真正估计几何关系还需要匹配点。LightGlue 输入两张图的局部特征，输出两张图之间的对应点。随后系统用这些对应点估计 Homography，并把几何变换应用到 SAR 图像上。

## 5. 为什么使用 SuperPoint -> ALIKED 级联？

**简短回答**：因为 SuperPoint 和 ALIKED 有互补性，级联可以提高边界样本中的可用匹配概率。

**展开回答**：实验中发现 SuperPoint 单独表现较好，但不是所有样本都稳定。ALIKED 在部分 SuperPoint 表现不好的样本上可以提供补充。因此系统默认先用速度较快、成功率较高的 SuperPoint，如果质量门控不理想，再用 ALIKED 接力，并按内点数、内点率和 RMSE 选择更可信的结果。

## 6. 为什么不用 DISK？

**简短回答**：在当前样本实验中，DISK 的成功率明显低于 SuperPoint 和 ALIKED，所以没有放入默认级联。

**展开回答**：方法选择不是凭主观判断，而是在同一批过滤样本上做了 matcher 对比。当前实验结果显示 SuperPoint 和 ALIKED 的表现更适合本项目场景，而 DISK 的可靠成功率较低。为了保证系统演示稳定性和推理效率，默认策略选择 SuperPoint -> ALIKED。

## 7. 结果怎么评价？

**简短回答**：主要看内点数、内点率、RMSE、匹配图、棋盘格和融合图。

**展开回答**：内点数说明有多少匹配点支持当前几何关系，内点率说明正确匹配比例，RMSE 反映像素级误差。除了数值指标，系统还展示 LightGlue 匹配图、SAR 对应点迁移图、配准后的 SAR、棋盘格对比和伪彩色融合图。这样既有定量指标，也有可视化证据。

## 8. 什么样的结果算可靠？

**简短回答**：需要满足内点数、内点率、RMSE、空间覆盖和 Homography 形状检查。

**展开回答**：如果只有少数点匹配成功，或者匹配点集中在图像一小块区域，即使 RMSE 看起来低也不一定可靠。因此系统引入质量门控，综合判断内点数量、内点比例、误差、空间分布和几何变换是否异常。这样可以避免错误配准被误判为成功。

## 9. 这个系统的局限是什么？

**简短回答**：主要局限是生成图质量、低纹理区域、Homography 模型假设和预训练模型泛化能力。

**展开回答**：如果 Fake Optical 生成的结构不稳定，LightGlue 会难以匹配。海洋、大水面、空白区域等低纹理样本本身缺少特征点，也很难配准。当前几何模型主要使用 Homography，对复杂地形或非刚性形变的适应能力有限。此外，预训练生成器在不同地区、季节和传感器条件下可能存在泛化问题。

## 10. 你的创新点在哪里？

**简短回答**：我的创新点主要是把扩散生成、LightGlue 配准、质量门控、参数搜索和 Web 展示整合成完整工程链路。

**展开回答**：本项目不是提出一个全新的扩散网络，而是面向 SAR-光学配准问题设计一条可运行、可评估、可展示的流程。具体工作包括：使用 Fake Optical 缓解跨模态差异，比较 SuperPoint/ALIKED/DISK，设计级联匹配策略，加入几何质量门控，过滤低纹理样本，搜索关键参数，并实现 Web 答辩演示系统。

## 11. 代码量怎么算？

**简短回答**：当前交付包核心有效源代码约 9.4k 行，不包含图片、CSV、模型权重、数据库、日志和缓存。

**展开回答**：代码量统计按 Python、JavaScript、CSS、HTML/Jinja、BAT 和 Shell 脚本计算。Python 部分包括 Flask 后端、扩散推理 worker、样本过滤、参数搜索和旧 CNN 探索代码；前端部分包括模板、样式和 Dashboard 交互逻辑。图片、实验 CSV、模型权重和运行缓存不是源码，所以不计入核心代码量。

## 12. 系统架构是什么？

**简短回答**：系统分为表现层、服务层、推理层、实验层和数据证据层。

**展开回答**：表现层负责 Web 页面和可视化，服务层用 Flask 提供上传、推理和样例接口，推理层负责扩散生成和 LightGlue 配准，实验层负责样本过滤和参数搜索，数据证据层负责内置样例和实验统计。这个架构让算法流程可以被页面调用、被指标评价、被导师复核。

## 13. 为什么要做 Web 系统？

**简短回答**：Web 系统让算法结果可交互、可展示、可复核，而不只是停留在离线脚本。

**展开回答**：如果只有命令行脚本，导师很难直观看到输入、生成图、匹配点和配准结果之间的关系。Web 系统把上传、推理、指标、匹配证据、质量门控和内置样例整合起来，更适合作为毕业设计系统演示。它也体现了工程集成工作量。

## 14. 为什么要做内置样例？

**简短回答**：内置样例保证答辩现场展示稳定，不受显卡和环境波动影响。

**展开回答**：实时推理依赖扩散模型、LightGlue、CUDA 和本地环境，现场可能存在不可控因素。内置样例保存了已经跑通的 SAR、Fake Optical、真实光学图、匹配图和配准结果，可以快速展示完整流程。自定义上传功能仍然保留，用于证明系统具备真实推理能力。

## 15. 如果导师问“你到底做了什么”，怎么回答？

**简短回答**：我完成了一套 SAR-光学跨模态配准系统，从数据筛选、扩散生成、LightGlue 匹配、参数搜索、质量门控到 Web 展示都做了工程实现。

**展开回答**：我的工作不是只运行一个模型，而是把多个环节组合成可运行流程。前期做了数据和样本质量筛选，中间实现了 SAR 到 Fake Optical 的生成和 LightGlue 配准，后期做了参数搜索、matcher 对比、边界案例分析和可视化系统。最终形成了可演示、可复核、能输出指标的毕业设计系统。

## 16. 如果结果有失败或待复核样本，怎么解释？

**简短回答**：这是跨模态配准的正常现象，所以系统加入了质量门控来识别边界案例。

**展开回答**：SAR-光学配准本身就不是所有样本都能稳定成功，特别是水面、低纹理区域和结构重复区域。项目没有简单隐藏这些问题，而是通过质量门控标记待复核结果，并用边界案例说明系统知道什么时候结果不可信。这比只展示成功图更符合工程实际。

## 17. 为什么参数不是手动随便选的？

**简短回答**：参数来自过滤样本上的参数搜索和 matcher 对比。

**展开回答**：项目先剔除海洋、大水面、空白和低纹理样本，再在过滤后的样本上比较扩散步数、特征点数量和 matcher 策略。最终默认配置是综合成功率、RMSE、内点数和推理效率后选择的。这样参数选择有实验依据，而不是肉眼试出来的。

## 18. 论文里应该怎么表述工作量？

**简短回答**：按“方法链路、工程系统、实验验证、结果展示”四个方面写。

**展开回答**：方法链路是扩散生成 + LightGlue 配准；工程系统是 Flask Web 控制台和推理接口；实验验证包括样本过滤、参数搜索、matcher 对比和质量门控；结果展示包括内置样例、匹配证据、棋盘格、融合图和指标表。这样比单纯说代码行数更能体现毕业设计工作量。
